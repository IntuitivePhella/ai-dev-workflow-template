# Handoffs

Store handoffs between agents or phases here when a task is complex enough to require durable coordination.

For small tasks, do not create handoff files. Use the final response format instead.

Use `ai/agents/HANDOFF.template.md` when a durable handoff is necessary.
